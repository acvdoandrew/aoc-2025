# Advent of Code 2025 - Rust

My solutions for Advent of Code 2025 in Rust.

- One crate, multiple binaries in `src/bin`
- Inputs are local-only and ignore via `.gitignore`
- Each day: `cargo run --bin dayXX`
